{
  "name": "release-readiness-demo",
  "version": "2.0.0",
  "private": true,
  "description": "Release-readiness dashboard with a Node.js REST API, Playwright automation, and GitHub Actions deployment.",
  "type": "module",
  "engines": {
    "node": ">=22"
  },
  "scripts": {
    "start": "node src/server.js",
    "test:unit": "node --test tests/unit/release-logic.test.js",
    "test:api": "playwright test tests/api",
    "test:ui": "playwright test tests/ui",
    "test:e2e": "playwright test",
    "test": "npm run test:unit && npm run test:e2e",
    "report": "playwright show-report"
  },
  "devDependencies": {
    "@playwright/test": "1.62.0"
  }
}
